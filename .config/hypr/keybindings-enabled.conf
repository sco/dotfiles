# Local keybindings enabled on top of the base Hyprland config.
# These are plain Hyprland translations of useful Omarchy-style bindings.

# Application launchers.
bind = $mainMod, RETURN, exec, alacritty -e tmux attach || alacritty -e tmux new -s main
bind = $mainMod ALT, RETURN, exec, alacritty
bind = $mainMod SHIFT, RETURN, exec, browser
bind = $mainMod SHIFT, B, exec, browser
bind = $mainMod SHIFT, N, exec, alacritty -e nvim
bind = $mainMod SHIFT, SLASH, exec, 1password
bind = $mainMod CTRL, T, exec, alacritty -e btop
bind = $mainMod CTRL, L, exec, hyprlock

# Universal clipboard shortcuts.
bind = $mainMod, C, sendshortcut, CTRL, Insert, activewindow
bind = $mainMod, V, sendshortcut, SHIFT, Insert, activewindow
bind = $mainMod, X, sendshortcut, CTRL, X, activewindow
bind = $mainMod CTRL, V, exec, clipboard-menu

# Window layout.
bind = $mainMod, W, killactive,
bind = $mainMod, F, fullscreen, 0
bind = $mainMod CTRL, F, fullscreenstate, 0 2
bind = $mainMod ALT, F, fullscreen, 1
bind = $mainMod, T, togglefloating,
bind = $mainMod, J, togglesplit,

# Rearrange windows in the current workspace.
bind = $mainMod SHIFT, left, swapwindow, l
bind = $mainMod SHIFT, right, swapwindow, r
bind = $mainMod SHIFT, up, swapwindow, u
bind = $mainMod SHIFT, down, swapwindow, d

# Workspace navigation.
bind = $mainMod, TAB, workspace, e+1
bind = $mainMod SHIFT, TAB, workspace, e-1
bind = $mainMod CTRL, TAB, workspace, previous

# Move the current workspace between monitors.
bind = $mainMod SHIFT ALT, left, movecurrentworkspacetomonitor, l
bind = $mainMod SHIFT ALT, right, movecurrentworkspacetomonitor, r
bind = $mainMod SHIFT ALT, up, movecurrentworkspacetomonitor, u
bind = $mainMod SHIFT ALT, down, movecurrentworkspacetomonitor, d

# Screenshots and picker.
bind = , PRINT, exec, grim -g "$(slurp)" - | satty --filename -
bind = $mainMod, PRINT, exec, pkill hyprpicker || hyprpicker -a
